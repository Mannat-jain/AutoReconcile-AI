fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20
pdfplumber==0.11.9
rapidfuzz==3.11.0

# Optional (only needed if OPENAI_API_KEY is set to enable the live
# Vision-LLM extraction path instead of the offline deterministic OCR
# fallback). No SDK import is required — parser.py calls the OpenAI
# REST API directly via urllib, so nothing extra to install here.

# Optional (only needed for a real, non-mock RazorpayX integration):
# razorpay==1.4.2
